finalCode: 
from machine import Pin, ADC, sleep, UART
from time import sleep
import utime

verde = Pin(19, Pin.OUT, value=0)
botao = Pin(36, Pin.IN)


import network
import ujson
import urequests
ssid = 'Rover'
password = 'Modelix2017'
wlan = network.WLAN(network.STA_IF)
wlan.active(True)
if not wlan.isconnected():
    print('connecting to network...')
    wlan.connect(ssid, password)
    while not wlan.isconnected():
        pass
print('network config:', wlan.ifconfig())

while(True):
    if(botao.value() == True):
        verde.off()
        url = 'https://zap.leonuzzy.repl.co/send_mail'
        headers = {'Content-Type': 'application/json'}
        data = {'to': 'luciohf@al.insper.edu.br', 'subject': 'Opa', 'text': 'lucio Parabens, vc recebeu um aumento de 100 a hora\ '}
        r = urequests.post(url, headers=headers, data=ujson.dumps(data))
        print(r.text)

    else:

        verde.on()



    utime.sleep_ms(50)