finalCode: 
from machine import Pin, ADC, sleep, UART
from time import sleep
import utime

vermelho = Pin(15, Pin.OUT, value=0)
botaodireita = Pin(35, Pin.IN)


import network
import ujson
import urequests
ssid = 'Rover'
password = 'Modelix2017'
wlan = network.WLAN(network.STA_IF)
wlan.active(True)
if not wlan.isconnected():
    print('connecting to network...')
    wlan.connect(ssid, password)
    while not wlan.isconnected():
        pass
print('network config:', wlan.ifconfig())

while(True):
    if(botaodireita.value() == True):
        url = 'https://zap.leonuzzy.repl.co/send_mail'
        headers = {'Content-Type': 'application/json'}
        data = {'to': 'luisasr@al.insper.edu.br', 'subject': 'Amigo', 'text': 'Olá amigo, bom dia.\ '}
        r = urequests.post(url, headers=headers, data=ujson.dumps(data))
        print(r.text)

        vermelho.off()
    else:

        vermelho.on()



    utime.sleep_ms(50)